# Database
# https://docs.djangoproject.com/en/3.1/ref/settings/#databases


DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': 'ayolayo_pos',
        'USER': 'postgres',
        'PASSWORD': 'b1o2l3u4',
        'HOST': 'localhost',
    }
}
